<Page x:Class="CatalogScolarOnline.Views.Acasa"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CatalogScolarOnline.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Acasa">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
           Text="Catalog Școlar Online - Cel mai bun mod de evaluare a activității școlare a copilului tău!"
           FontSize="24"
           FontWeight="Bold"
           Foreground="Black"
           Background="Transparent"        
           HorizontalAlignment="Center"
           Margin="10"
           TextWrapping="Wrap"/>
        
        <Image Grid.Row="1"
            Source="../Images/noteincatalog.jpeg"
           Stretch="UniformToFill"
           HorizontalAlignment="Stretch"
           VerticalAlignment="Stretch" />

        
    </Grid>

</Page>
